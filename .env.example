# Django
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=replace-with-a-long-random-value
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,[::1]
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost:8000
DJANGO_SECURE_SSL_REDIRECT=False
CADRIA_PORT=8000
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# PostgreSQL (Docker Compose)
POSTGRES_DB=cadria
POSTGRES_USER=cadria
POSTGRES_PASSWORD=local-development-only
DATABASE_URL=postgresql://cadria:local-development-only@db:5432/cadria

# Redis / Celery
REDIS_URL=redis://cache:6379/0
CELERY_TASK_ALWAYS_EAGER=False

# Fournisseur IA. `ollama` est le défaut : un vrai modèle tourne en local, sans
# clé et sans envoyer les briefs hors de la machine. `demo` reste disponible
# pour un onboarding encore plus léger (aucun téléchargement).
AI_PROVIDER=ollama
AI_API_KEY=
AI_BASE_URL=https://api.mistral.ai/v1
AI_MODEL=demo-cadria-v1
AI_TIMEOUT_SECONDS=60
AI_MAX_INPUT_CHARS=12000
AI_PROMPT_VERSION=v1

# Fournisseur de secours si le fournisseur principal échoue (indisponibilité,
# quota, réponse invalide, etc.). Vide = aucun secours, comportement identique
# à avant. `groq` a besoin d'AI_API_KEY pour être réellement utilisable ; sans
# clé, le secours échoue silencieusement à son tour et l'erreur d'origine du
# fournisseur principal est celle renvoyée, comme si ce réglage était vide.
AI_FALLBACK_PROVIDER=groq

# Groq (API compatible OpenAI). Utilisé avec AI_PROVIDER=groq, ou comme secours
# via AI_FALLBACK_PROVIDER=groq. La clé reste AI_API_KEY et ne doit jamais être
# commitée.
GROQ_BASE_URL=https://api.groq.com/openai/v1
GROQ_MODEL=openai/gpt-oss-20b
GROQ_TIMEOUT_SECONDS=60

# Ollama local. Utilisé avec AI_PROVIDER=ollama. Le conteneur ollama télécharge
# automatiquement OLLAMA_MODEL au démarrage s'il est absent : aucune commande
# manuelle n'est nécessaire.
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=qwen2.5:0.5b
OLLAMA_TIMEOUT_SECONDS=120
OLLAMA_NUM_CTX=4096
OLLAMA_KEEP_ALIVE=1m
OLLAMA_PORT=11434
OLLAMA_MEMORY_LIMIT=2g
OLLAMA_CPU_LIMIT=2.0

# Démarrage du conteneur web uniquement
RUN_STARTUP_TASKS=True

# Active le profil Compose `ollama` par défaut, pour que `docker compose up`
# démarre aussi le conteneur ollama sans avoir à passer --profile ollama.
# Retirer cette ligne (ou la vider) si un fournisseur distant (Groq, Mistral)
# est utilisé et que le conteneur ollama n'est pas souhaité.
COMPOSE_PROFILES=ollama
